import {createMuiTheme} from '@material-ui/core/styles';

const Theme = createMuiTheme({
    pallete :{
        primary:{    
            light: "#63a4ff",
            main: "#1976d2",
            dark: "#004ba0",
            contrastText: "#ecfad8"
        }
    }
});

export default Theme;